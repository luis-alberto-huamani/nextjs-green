module.exports = class FriendReq{
  constructor({id, url, fullName, perfilImg, frontPageQuote }){
    this.id = id
    this.url = url
    this.fullName = fullName,
    this.perfilImg = perfilImg,
    this.frontPageQuote = frontPageQuote
  }
};
